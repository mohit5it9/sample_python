
# Language setting
language: python

# Version number
python:
  #- 2.6
  - 3.5

build:
  ci:
    - python --version
    - ls
    - cat /tmp/ssh/premkey
integrations:
  key:
    - integrationName: pemkey
      type: pemKey
