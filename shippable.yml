
# Language setting
language: python

# Version number
python:
  #- 2.6
  - 3.5

build:
  ci:
    - python --version
    - cat /tmp/ssh/pemkeynew
    - cat /tmp/ssh/pemkeynew >> key.pem
    - ls -a
    - cat key.pem
integrations:
  key:
    - integrationName: pemkeynew
      type: pemKey
      
