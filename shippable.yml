
# Language setting
language: python

# Version number
python:
  #- 2.6
  - 3.5

build:
  ci:
    - python --version
    - ls -a
    - cat /tmp/ssh/pemkey
integrations:
  key:
    - integrationName: pemkey
      type: pemKey
