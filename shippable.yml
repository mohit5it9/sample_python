
# Language setting
language: python

# Version number
python:
  #- 2.6
  - 3.5

build:
  ci:
    - python --version
    - echo 
integrations:
  key:
    - integrationName: pemkey
      type: pem-key
